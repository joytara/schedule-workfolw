name: schedule jobs

on:
  workflow_dispatch: 
  schedule:
    - cron: "0 0 * * *"

jobs: 
  run-script: 
    runs-on: ubuntu-latest

    steps:
      - name: print message
        run: echo "schedule workflow executed"
